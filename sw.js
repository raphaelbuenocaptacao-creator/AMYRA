const CACHE_PREFIX='amyra-';
const CACHE=`${CACHE_PREFIX}v3-safe-shell`;
const ASSETS=['./','./index.html','./manifest.webmanifest','./icon-192.svg','./icon-512.svg','./icon-maskable.svg'];
const SENSITIVE=/([?&](token|access_token|refresh_token|password|passwd|session|code|credential|credentials|api[_-]?key|secret)=)|\/(api|auth|login|logout|session|account|profile)(\/|$)/i;
const canCacheResponse=r=>r&&r.ok&&r.type==='basic'&&!/private|no-store/i.test(r.headers.get('cache-control')||'')&&!r.headers.has('set-cookie');
self.addEventListener('install',event=>event.waitUntil((async()=>{const cache=await caches.open(CACHE);for(const asset of ASSETS){try{const response=await fetch(asset,{credentials:'omit',cache:'no-store'});if(canCacheResponse(response))await cache.put(asset,response.clone())}catch(_){}}await self.skipWaiting()})()));
self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith(CACHE_PREFIX)&&key!==CACHE)await caches.delete(key);await self.clients.claim()})()));
self.addEventListener('fetch',event=>{const req=event.request;if(req.method!=='GET')return;const url=new URL(req.url);if(url.origin!==self.location.origin)return;if(req.headers.has('authorization')||req.headers.has('cookie')||SENSITIVE.test(url.href))return;if(req.mode==='navigate'){event.respondWith((async()=>{try{return await fetch(req,{cache:'no-store'})}catch(_){return (await caches.match('./index.html'))||Response.error()}})());return;}if(!ASSETS.some(asset=>new URL(asset,self.location.href).href===url.href))return;event.respondWith((async()=>{const cached=await caches.match(req);if(cached)return cached;const response=await fetch(req,{credentials:'omit'});if(canCacheResponse(response)){const cache=await caches.open(CACHE);await cache.put(req,response.clone())}return response})())});